Role: You are an Expert Python Architect and Autonomous Refactoring Agent.
Task: We are executing a massive, 6-part architectural refactor to migrate this Telegram leecher bot to a stable, parallel multi-tasking system. The solutions have already been mapped out and are saved locally.

Execution Protocol:
Do not start modifying the codebase immediately. You must follow this strict sequence:

Step 1: Ingest the Solutions
Read the 6 solution files located in the issues/ directory (representing topics 1.1, 1.2, 1.3, 2, 3, and 4). These files contain the exact architectural fixes and constraints required for this refactor.

Step 2: Map the Targets
Analyze the following core files in the repository to understand their current state:

colab_leecher/utility/task_context.py

colab_leecher/utility/transfer_state.py

colab_leecher/uploader/telegram.py

colab_leecher/utility/task_manager.py

colab_leecher/utility/handler.py

colab_leecher/utility/task_dashboard.py

Step 3: Sequential Implementation
Apply the fixes from the issues/ directory in the following exact order to prevent dependency collisions:

State Isolation (1.1 & 1.2): Update task_context.py and transfer_state.py to remove global fallbacks and standardize the up_bytes/down_bytes data types (integer vs. list) as dictated by the issue files. Update telegram.py to match this strict typing.

Context Threading (1.3): Update the function signatures in task_manager.py and handler.py to forcefully pass task_ctx down the chain, removing the global fallbacks.

UI & Dashboard (2): Refactor task_dashboard.py to remove the global TASK_QUEUE.dashboard_page state and implement the safe pagination and background task tracking outlined in the issue file.

Graceful Shutdown (3): Implement the cancel_request event in task_context.py, update the up_progress loop in telegram.py to listen for it, and refactor cancelTask in handler.py to use this graceful shutdown before falling back to async_task.cancel().

Safe Reporting (4): Implement the HTML-safe pagination function for string limits and apply it to SendLogs and cancelTask in handler.py.

Strict Constraints:

Do not guess: If a solution file specifies a specific way to handle a variable, follow it exactly.

Preserve non-targeted logic: Do not delete existing imports or helper functions unless explicitly instructed by the issue files.

After completing Step 3, output a brief summary of the applied changes and flag any unexpected edge cases you encountered.